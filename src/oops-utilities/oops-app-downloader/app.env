APP_NAME=oops-app-downloader
EXPLICIT_TITLE_ID=1
TITLE_ID=OADL00001
TITLE_NAME="OOPS App Downloader"
APP_SUBTITLE="Browse and install oops-apps homebrew directly on the console"
TITLE_CATEGORY=big-app
KIND=utility
TITLE_VERSION=01.00
# `elf` only until the on-device fetch + unpack land (see README): it ships no working title
# yet, so it stays out of the index. Becomes `elf title` when it can install for real.
FORMATS=elf
