APP_NAME=oopsy-daisy
EXPLICIT_TITLE_ID=1
TITLE_ID=OPSY00001
TITLE_NAME="OOPSy-daisy"
APP_SUBTITLE="Install oops-apps homebrew onto the console, no PC needed"
TITLE_CATEGORY=big-app
KIND=utility
TITLE_VERSION=01.00
# `elf` until the pipeline is proven on hardware (it needs the console's secure-transport
# module, pending target recovery); then it becomes `elf title` and installs itself.
FORMATS=elf
