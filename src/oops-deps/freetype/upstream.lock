# FreeType's origin. Read by `common/upstream-fetch.sh`, shared with every dependency here.
#
# An annotated tag, so the hash is the peeled commit: `refs/tags/VER-2-13-3` is the tag object
# `534ad345...` and `^{}` is the commit below.
#
# Extreme Tux Racer includes `ft2build.h` and ships TrueType fonts; SDL2_ttf stands on FreeType
# too, so it lives in `oops-deps` rather than in one title. With PNG, zlib, brotli and HarfBuzz
# off (`include/ftoption-oops.h`) it is plain C with no dependency of its own.
UPSTREAM_KIND=git
UPSTREAM_URL=https://github.com/freetype/freetype
UPSTREAM_REV=42608f77f20749dd6ddc9e0536788eaad70ea4b5
UPSTREAM_REF=VER-2-13-3
