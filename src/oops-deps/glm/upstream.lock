# glm's origin. Read by `common/upstream-fetch.sh`, fetched by `common/deps-fetch.sh`.
#
# `0.9.9.8` is the release SuperTux `v0.6.3` was built against. SuperTux's `math/vector.hpp`
# includes the experimental `gtx/io.hpp`, whose contract glm does not keep across releases.
#
# A lightweight tag: `refs/tags/0.9.9.8` has no peeled `^{}` line, so the hash is the commit.
#
# Header-only. `UPSTREAM_SPARSE` takes the headers and leaves tests, docs and CMake behind.
UPSTREAM_KIND=git
UPSTREAM_URL=https://github.com/g-truc/glm
UPSTREAM_REV=bf71a834948186f4097caa076cd2663c69a10e1e
UPSTREAM_REF=0.9.9.8

UPSTREAM_SPARSE=glm
