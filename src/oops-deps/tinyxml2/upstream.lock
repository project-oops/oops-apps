# tinyxml2's origin, pinned to the revision `libultraship` asks for (`GIT_TAG 11.0.0`).
#
# A lightweight tag, so the ref and the commit are the same object.
#
# One `.cpp` and one `.h`, included throughout Ship of Harkinian.
UPSTREAM_KIND=git
UPSTREAM_URL=https://github.com/leethomason/tinyxml2
UPSTREAM_REV=9148bdf719e997d1f474be6bcc7943881046dba1
UPSTREAM_REF=11.0.0
