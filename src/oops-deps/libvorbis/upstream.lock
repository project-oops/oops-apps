# libvorbis's origin, and with it `vorbisfile` - which is what Neverball actually calls.
#
# An annotated tag; the hash below is the peeled commit.
#
# `sdl2-mixer` decodes Ogg through stb_vorbis; this serves programs such as Neverball that call
# the `vorbisfile` API (`ov_open_callbacks`, `ov_read`, `ov_info`) directly.
UPSTREAM_KIND=git
UPSTREAM_URL=https://github.com/xiph/vorbis
UPSTREAM_REV=0657aee69dec8508a0011f47f3b69d7538e9d262
UPSTREAM_REF=v1.3.7
