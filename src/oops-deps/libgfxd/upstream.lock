# libgfxd's origin, pinned to the commit `libultraship` names; upstream has no releases.
#
# A decoder for N64 display lists (F3D and its dialects), in C. libultraship uses it to print a
# display list for the debugger.
UPSTREAM_KIND=git
UPSTREAM_URL=https://github.com/glankk/libgfxd
UPSTREAM_REV=008f73dca8ebc9151b205959b17773a19c5bd0da
UPSTREAM_REF=008f73dc (no releases upstream)
