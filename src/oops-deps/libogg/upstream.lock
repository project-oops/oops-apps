# libogg's origin: the container format libvorbis packs its audio into.
#
# An annotated tag; the hash below is the peeled commit.
#
# Pinned separately from libvorbis, which upstream ships separately and builds against it.
UPSTREAM_KIND=git
UPSTREAM_URL=https://github.com/xiph/ogg
UPSTREAM_REV=e1774cd77f471443541596e09078e78fdc342e4f
UPSTREAM_REF=v1.3.5
