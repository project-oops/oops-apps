# libpng's origin. Neverball decodes its textures with it.
#
# An annotated tag: the hash below is the peeled commit.
#
# `oops_png_decode` in `oops-sdk/src/draw/png.c` serves a payload that wants a texture in one
# call; this serves programs written against libpng's API (`png_create_read_struct`).
UPSTREAM_KIND=git
UPSTREAM_URL=https://github.com/pnggroup/libpng
UPSTREAM_REV=3061454d980de7d53608f594194cfac722721d2a
UPSTREAM_REF=v1.6.58
