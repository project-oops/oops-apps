# sdl12-compat's origin. Read by `common/upstream-fetch.sh`.
#
# UPSTREAM_REV is a full commit hash, since a tag can be moved. UPSTREAM_REF names the release
# for a reader. `release-1.2.76` is a lightweight tag, so the hash is the commit itself.
UPSTREAM_KIND=git
UPSTREAM_URL=https://github.com/libsdl-org/sdl12-compat
UPSTREAM_REV=293b4cbd9eea61b18b9c9425d0c9200bff037360
UPSTREAM_REF=release-1.2.76
