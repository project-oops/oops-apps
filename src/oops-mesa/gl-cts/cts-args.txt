# Arguments for the CTS run, one per line. Staged to /app0 by `make stage-args`.
#
# The case subset is chosen here at run time: editing this file and restoring it changes what
# executes without rebuilding. `shim/gl_cts_entry.c` reads it before anything touches /data,
# after which /app0 no longer resolves.
#
# Give one `--deqp-case`, with commas separating patterns: dEQP refuses any repeated option,
# including `--deqp-log-filename` and `--deqp-archive-dir`, which the entry point already
# passes. Group names come from the `TestCaseGroup(...)` constructors in the sources.
#
# With no file the entry point runs `KHR-GL30.info.*`. `KHR-GL46.info.*` asks the platform for
# a 4.6 context, so it either runs or reports that the context was refused.
--deqp-case=KHR-GL46.info.*
