# The fetch, not build output: `make clean` leaves it alone and so does git.
upstream/
build/
# Generated by tools/gen-gl-loader.py from the pinned .inl files and the Mesa archive: it
# depends on a build of oops-mesa, so it is regenerated rather than committed.
shim/gl_loader_table.cpp
