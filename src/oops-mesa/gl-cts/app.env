APP_NAME=gl-cts
EXPLICIT_TITLE_ID=1
TITLE_ID=GCTS00001
TITLE_NAME="OpenGL CTS"
APP_SUBTITLE="Khronos conformance tests, run on the console and reported as data"
TITLE_CATEGORY=big-app
KIND=probe
TITLE_VERSION=01.00
FORMATS=title
ENTRY_POINT=gl_cts_start
# Held out of CI: the Khronos CTS upstream needs hosted headers the sysroot does not carry yet
# (fenv.h), so it does not build. Filed for the sysroot on the oops-mesa bus; drop this when it
# lands.
CI_SKIP=1
