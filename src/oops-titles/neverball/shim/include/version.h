/*
 * Neverball's build generates this from git describe. The pinned commit is the release
 * `upstream.lock` records as `UPSTREAM_REF`. `ball/main.c` shows it in the window title
 * and `--version`.
 */
#ifndef OOPS_NEVERBALL_VERSION_H
#define OOPS_NEVERBALL_VERSION_H

#define VERSION "1.6.0"

#endif
