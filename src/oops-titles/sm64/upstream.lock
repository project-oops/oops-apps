# Super Mario 64's origin - `sm64-port`, the PC port of the `n64decomp/sm64` decompilation.
#
# `n64decomp/sm64` builds an N64 ROM and has no platform layer; `sm64-port` adds `src/pc`,
# and its `src/pc/gfx/` is the graphics backend this title ports.
#
# Pinned to a commit on the default branch: the project has no releases, and its only tags
# (`irix`, `irix2`) mark other work.
UPSTREAM_KIND=git
UPSTREAM_URL=https://github.com/sm64-port/sm64-port
UPSTREAM_REV=2b17d081c9798b31b91dc71f37994b0da28cffc9
UPSTREAM_REF=main@2026-09-25
