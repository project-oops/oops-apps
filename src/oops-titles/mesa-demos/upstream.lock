# mesa-demos' origin: the Mesa project's own demonstration programs, one per GL feature.
#
# Pinned to the 9.0.0 tag so measurements are comparable. The hash is the peeled tag
# (`refs/tags/mesa-demos-9.0.0^{}`), which is what a checkout reports as `rev-parse HEAD`.
# No sparse list: the whole tree is small.
UPSTREAM_KIND=git
UPSTREAM_URL=https://gitlab.freedesktop.org/mesa/demos.git
UPSTREAM_REV=661681767bfb4e8f136aac1fd7934e6b0fa46d29
UPSTREAM_REF=mesa-demos-9.0.0
