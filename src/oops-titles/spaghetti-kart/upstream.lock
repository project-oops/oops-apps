# Spaghetti Kart's origin - Harbour Masters' native port of Mario Kart 64.
#
# It runs on `libultraship`, the same runtime as `../ship-of-harkinian`. The graphics live
# entirely inside `libultraship`, so the platform work is shared between the two titles.
#
# Pinned to the `1.0.0` release (2026-02-25). It is a lightweight tag, so the ref and the
# commit are the same object.
UPSTREAM_KIND=git
UPSTREAM_URL=https://github.com/HarbourMasters/SpaghettiKart
UPSTREAM_REV=1732542a6a200a21a9112a75820517a754d9514a
UPSTREAM_REF=1.0.0

# `libultraship` (the runtime), `torch` (the asset processor) and a documentation theme. The
# build needs them, and the pinned commit records their revisions.
UPSTREAM_SUBMODULES=1
