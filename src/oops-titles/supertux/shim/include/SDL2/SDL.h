/*
 * `<SDL2/SDL.h>`, which `supertux/screen_manager.hpp` includes with the directory
 * prefix. oops-sdl2's include path is SDL's own `include/`, so the prefixed form is
 * forwarded.
 */
#include <SDL.h>
